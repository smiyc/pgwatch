query: "select /* pgwatch_generated */\n  (extract(epoch from now()) * 1e9)::int8\
  \ as epoch_ns,\n  p.oid::text as tag_oid,\n  quote_ident(nspname)||'.'||quote_ident(proname)\
  \ as tag_sproc,\n  md5(prosrc)\nfrom\n  pg_proc p\n  join\n  pg_namespace n on n.oid\
  \ = pronamespace\nwhere\n  not nspname like any(array[E'pg\\\\_%', 'information_schema'])"
metrics: []
